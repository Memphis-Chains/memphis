# ── Dependencies ──────────────────────────────────────────────────────────────
node_modules/
dist/
coverage/
.tools/

# ── Secrets & credentials (NEVER publish) ────────────────────────────────────
.env
.env.*
!.env.example
*.secret
*.pem
*.key
credentials/
.config/memphis/tokens/
.claude/settings.local.json

# ── Runtime state (generated, not source) ────────────────────────────────────
*.db
*.db-shm
*.db-wal
*.log
*.jsonl
data/security-audit.jsonl
data/queue.wal
data/chain-indexes.json
data/vault-state.json
data/vault-entries.json
data/incident-bundle*.json
data/incident-bundle*.manifest.json
chains/
.memphis/
compose/synapse/homeserver.generated.yaml

# ── Rust build ───────────────────────────────────────────────────────────────
/target/

# NAPI bridge binary. Rebuilt on demand by:
#   - package.json `predev` hook
#   - ops/memphis.service `ExecStartPre`
#   - scripts/install.sh + scripts/bootstrap.sh
# Historically committed 4× (~156 MB of blob bloat in git history before
# this PR). Nothing reads the tracked copy — every install path rebuilds.
crates/memphis-napi/index.node

# ── Benchmarks & reports (generated) ─────────────────────────────────────────
data/phase08/
data/retrieval-benchmark-reports/
data/retrieval-benchmark-baseline*.json
data/retrieval-benchmark-history.json
data/cli-tui-latency-benchmark-reports/
crates/memphis-napi/data/embed-index.json
data/embed-index.json
data/tui-observability.json

# ── Models & training (LLM workstation) ──────────────────────────────────────
models/
training-data/
*.onnx
*.safetensors
*.gguf
*.bin
*.pt
*.pth

# ── OS / editor ──────────────────────────────────────────────────────────────
.DS_Store
.vscode/
.idea/
*.swp
*~

# ── Internal backups ─────────────────────────────────────────────────────────
.git.backup*
.memphis-intake/
dump---*.zip
undefined/
CONTEXT_RESET_HANDOFF_v*.md
WHAT_IS_LEFT_TO_DO.md

# ── Local-only strategic packs (operator workspace) ──────────────────────────
# Contains business ops, grant plans, pricing, founder context.
# Export: /home/memphis/watra.zip for transfer between devices.
_Watra/
watra.zip

# ── Claude Code agent state (per-session, not source) ────────────────────────
.claude/scheduled_tasks.lock
.claude/projects/

# ── Temporary scratch ────────────────────────────────────────────────────────
*.tmp
*.bak

# ── Data dir (security audits, vault backups, runtime state) ─────────────────
data/security-audit-archives/
data/vault-bak-*/
data/retrieval-benchmark-corpus-v*.json
data/retrieval-benchmark-overlays/

# Python compiled bytecode (corpus + training tools)
tools/**/__pycache__/
*.pyc
