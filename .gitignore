# ── Dependencies ──────────────────────────────────────────────────────────────
node_modules/
dist/
coverage/
.tools/

# ── Secrets & credentials (NEVER publish) ────────────────────────────────────
.env
.env.*
!.env.example
*.secret
*.pem
*.key
credentials/
.config/memphis/tokens/
.claude/settings.local.json

# ── Runtime state (generated, not source) ────────────────────────────────────
*.db
*.db-shm
*.db-wal
*.log
*.jsonl
data/security-audit.jsonl
data/queue.wal
data/chain-indexes.json
data/vault-state.json
data/vault-entries.json
data/incident-bundle*.json
data/incident-bundle*.manifest.json
chains/
.memphis/
compose/synapse/homeserver.generated.yaml

# ── Rust build ───────────────────────────────────────────────────────────────
/target/

# NAPI bridge binary. Rebuilt on demand by:
#   - package.json `predev` hook
#   - ops/memphis.service `ExecStartPre`
#   - scripts/install.sh + scripts/bootstrap.sh
# Historically committed 4× (~156 MB of blob bloat in git history before
# this PR). Nothing reads the tracked copy — every install path rebuilds.
crates/memphis-napi/index.node

# ── Benchmarks & reports (generated) ─────────────────────────────────────────
data/phase08/
data/retrieval-benchmark-reports/
data/retrieval-benchmark-baseline*.json
data/retrieval-benchmark-history.json
data/cli-tui-latency-benchmark-reports/
crates/memphis-napi/data/embed-index.json
data/embed-index.json
data/tui-observability.json

# ── Models & training (LLM workstation) ──────────────────────────────────────
models/
training-data/
*.onnx
*.safetensors
*.gguf
*.bin
*.pt
*.pth

# ── OS / editor ──────────────────────────────────────────────────────────────
.DS_Store
.vscode/
.idea/
*.swp
*~

# ── Internal backups ─────────────────────────────────────────────────────────
.git.backup*
.memphis-intake/
dump---*.zip
undefined/
CONTEXT_RESET_HANDOFF_v*.md
WHAT_IS_LEFT_TO_DO.md

# ── Local-only strategic packs (operator workspace) ──────────────────────────
# Contains business ops, grant plans, pricing, founder context.
# Export: /home/memphis/watra.zip for transfer between devices.
_Watra/
watra.zip

# ── Claude Code agent state (per-session, not source) ────────────────────────
.claude/scheduled_tasks.lock
.claude/projects/

# ── Operator deferral artifacts (per-machine, not source) ────────────────────
# Phase 4.4 (autopilot 2026-05-08): the simple-reminder.sh script appeared
# untracked in two consecutive sessions; commit 4b3a4037 explicitly untracked
# it once. Same pattern as scheduled_tasks.lock — operator-machine-local,
# never source. Memory: feedback_local_artifacts_never_stage.
crons/simple-reminder.sh
# 2026-05-12 audit: telegram-insights-push.sh appeared as untracked across
# multiple sessions (operator's 6-hourly Telegram health-report cron on
# this machine). Same per-machine-local pattern as simple-reminder.sh.
crons/telegram-insights-push.sh

# ── Coredumps & vault-state backups (NEVER publish) ──────────────────────────
# Operator generates these regularly via systemd-coredump + pre-incident
# tar snapshots (e.g. 2026-05-11 vault recovery, 2026-05-12 SEGV). A
# coredump or vault-state tarball at repo root would leak the entire
# operator vault key material + decrypted secrets. Hard-block at every
# path.
memphis-coredump-*
memphis-pre-*.tar.*
memphis-pre-*.zst
live-memphis-*/
vault-broken-*/
vault-pre-reinit-*/

# ── Local direnv / shell config (per-machine, may carry secrets) ─────────────
.envrc
.envrc.*

# ── Merge conflict / rebase artifacts ────────────────────────────────────────
*.orig
*.rej

# ── Temporary scratch ────────────────────────────────────────────────────────
*.tmp
*.bak

# ── Data dir (security audits, vault backups, runtime state) ─────────────────
data/security-audit-archives/
data/vault-bak-*/
data/vault-corrupt-*/
data/retrieval-benchmark-corpus-v*.json
data/retrieval-benchmark-overlays/

# ── One-off audit/inventory snapshots (regenerable) ──────────────────────────
inventory/
memphis-test-report.md

# Python compiled bytecode (corpus + training tools)
# 2026-05-12 audit: broadened from tools/**/ to global — Python bytecode
# can land anywhere a venv runs (status_writer tests, future scripts).
__pycache__/
*.pyc
*.pyo
*.pyd

# Closure sprint Z.7 (2026-05-09): health-watch.sh writes JSON run
# records and incident reports to ~/.memphis/. They're operator-local
# observability artifacts, never source-of-truth.
data/stability-runs/
data/stability-incidents/
data/demo-armed.json
data/demo-plan-b.json
