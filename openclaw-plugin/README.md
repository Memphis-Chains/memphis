# Archived OpenClaw Plugin

This directory is archived downstream-only reference material.

It is not part of the active Memphis `v1.x` release surface, does not produce
`dist/` artifacts, and is not publishable from this repository.

Use the standalone Memphis runtime and the active operator docs in the repo
root instead of this deprecated OpenClaw scaffold.
